"""
UI Coordinate Map — Static positions for your game's UI elements.

All coordinates are in device pixel space (e.g., 1440x2560 for BlueStacks portrait).
Calibrate these by taking a screenshot and noting button positions.

Tip: Use an image editor to find exact pixel coordinates of UI elements.
"""

# Bottom navigation tabs
TABS = {
    # "tab_name": (x, y),
}

# Popup buttons (close, confirm, cancel)
POPUP = {
    # "close_x": (x, y),
    # "confirm": (x, y),
    # "cancel": (x, y),
}

# Building positions on city map
BUILDINGS = {
    # "building_name": (x, y),
}

# Building menu buttons
BUILDING_MENU = {
    # "upgrade_button": (x, y),
    # "confirm_upgrade": (x, y),
}

# Add more coordinate groups as needed for your game
